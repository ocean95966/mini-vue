import {createApp} from './createApp'
import { h } from './h'

import {ref, reactive, effect, isReactive, isProxy} from '@vue/reactivity'

export {
    createApp,
    h,
    ref,
    reactive,
    isReactive,
    effect,
    isProxy
}